# Copy this file to .env and set a persistent key in production.
# Generate one with Python:
# >>> import base64, os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())
FERNET_KEY=PUT_YOUR_BASE64_URLSAFE_KEY_HERE
